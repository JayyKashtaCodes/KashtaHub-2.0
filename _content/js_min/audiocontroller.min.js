function togglePopup(){const popup=$('#audioPopup');var showAudioBtn=$('#audioPlayerBtn');if(popup.is(':visible')){showAudioBtn.text('Show Audio Player')}else{showAudioBtn.text('Hide Audio Player')}
popup.slideToggle('slow')}
var yt_playlist;if(yt_playlist==null){var plaudio;function isPlaying(plaudio){return!plaudio.paused}
$(document).ready(function(){$('audio').playlistParser({proxy:'/_content/audio/audiostream.php',navArrows:!0})});var audio=document.getElementById('myAudio');function setVolume(){var volume=document.getElementById('volume').value;audio.volume=volume}
function togglePlayStop(){var playStopBtn=document.getElementById('playStopBtn');if(!isPlaying(audio)){audio.play();playStopBtn.textContent='⏹️'}else{audio.pause();playStopBtn.textContent='▶️'}}
document.getElementById('volume').value=0.5;audio.volume=0.5}